 * add registry support 
 * move blocks into functions
 * add custom image
 * make use of mirror links when download fails + rsync files to sourceforge.net?
 * redownload file if md5 is wrong
 * disallow .. in paths
 * always download included .ini files + add "offline-mode" switch? (or force download?)
 * check if file to install already exists on destination + md5 is ok, if so, don't download it
